# Bring-up overlay: debug logging, all the way down to the bootloader.
# It is how the PSRAM stall recorded in sdkconfig.psram was located.
CONFIG_LOG_DEFAULT_LEVEL_DEBUG=y
CONFIG_BOOTLOADER_LOG_LEVEL_DEBUG=y
