LogLines$ = SPLIT(TXTREADER$("logfile.txt"), VBNEWLINE)
Timestamp$ = NOW() + " -- "

' 1. Insert the timestamp at the beginning of EVERY line in the array
NewLogLines$ = INSERT$(LogLines$, Timestamp$, 0) + VBNEWLINE

' Print the final, modified array.
PRINT FRMV$(NewLogLines$)
