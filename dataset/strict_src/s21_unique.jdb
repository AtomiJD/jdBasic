' Remove duplicates from an array with UNIQUE and report how many
' duplicates were dropped.
OPTION "EXPLICIT"
OPTION "STRICT"
DIM nums = [3, 1, 4, 1, 5, 9, 2, 6, 5, 3, 5]
DIM distinct = UNIQUE(nums)
PRINT "distinct: "; distinct
PRINT "dropped : "; LEN(nums) - LEN(distinct)
