' Pair every element with its 0-based index using ENUMERATE and print
' index and value.
OPTION "EXPLICIT"
OPTION "STRICT"
DIM animals = ["cat", "dog", "owl"]
DIM pairs = ENUMERATE(animals)
DIM i = 0
DIM pr = []
FOR i = 0 TO LEN(pairs) - 1
    pr = pairs[i]
    PRINT pr[0]; ": "; pr[1]
NEXT i
