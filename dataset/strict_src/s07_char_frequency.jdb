' Count how often each character occurs in the word banana using TALLY
' and print each character with its count.
OPTION "EXPLICIT"
OPTION "STRICT"
DIM freq = TALLY(-"banana")
DIM i = 0
DIM pair = []
FOR i = 0 TO LEN(freq) - 1
    pair = freq[i]
    PRINT pair[0]; " -> "; pair[1]
NEXT i
