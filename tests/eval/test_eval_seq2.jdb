FUNC Calc(expression)
  DIM r AS STRING = EVAL(expression)
  RETURN STR(r)
ENDFUNC

FUNC Outer(x)
  PRINT "Outer entered, x="; x
  DIM y AS STRING = EVAL("100 + " + STR(x))
  PRINT "Outer y="; y
  RETURN y
ENDFUNC

PRINT "second call only:"
PRINT Outer(5)
