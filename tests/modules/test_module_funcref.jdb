IMPORT STORAGE
IMPORT PROVIDER

' Direct param funcref — should work per docs.
PRINT "APPLY_PARAM PROVIDER.TRIPLE@(7) = "; STORAGE.APPLY_PARAM(PROVIDER.TRIPLE@, 7)

' Stored module-state funcref.
STORAGE.SET_CB(PROVIDER.TRIPLE@)
PRINT "GET_CB() = "; STORAGE.GET_CB()
PRINT "APPLY_STORED(7) = "; STORAGE.APPLY_STORED(7)
