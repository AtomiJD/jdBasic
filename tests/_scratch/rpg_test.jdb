EXPORT MODULE RPG_TEST

EXPORT TYPE T_THING
    path$

    SUB INIT(data, asset_path$)
        file$ = asset_path$ + "fallback.png"
        IF MAP.EXISTS(data, "sprite") THEN file$ = asset_path$ + data{"sprite"}
        THIS.path$ = file$
    ENDSUB
ENDTYPE
